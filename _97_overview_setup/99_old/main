# === general links === #
# Github hexaf folder: https://github.com/uclmr/fever
# James Thorne folder: https://github.com/j6mes/fever-uclmr-system

# === login === #

ssh -X bjm62@gate.eng.cam.ac.uk
ssh -X mlsalt-login

let PORT="60000"
echo $PORT

ssh -L 60001:localhost:60001 bjm62@gate.eng.cam.ac.uk
ssh -L 60001:localhost:60001 bjm62@mlsalt-cpu2
jupyter notebook --no-browser --port=60001

# === create folder system === #
mkdir 01_code
cd 01_code
mkdir 01_main
mkdir 02_hexaf
cd 01_main

git init
git remote add origin https://github.com/BartMelman/fact_checking
git pull origin master

cd fever
sudo snap install docker

docker build -t fever-uclmr .
$ conda update -n base -c defaults conda

sudo groupadd docker
sudo usermod -aG docker $USER






# === import hexaf system === #
cd 02_hexaf
git clone https://github.com/j6mes/fever-uclmr-system
cd fever-uclmr-system
# ls .*
rm -r .git
mv .gitignore ../.
cd ..
mv fever-uclmr-system/ fever/
cd ..
cp -r 02_hexaf/fever 01_main

git status
git add .
git commit -m "fever code James Thorne"
git push -u origin master


# === download code === #

git init
git remote add origin https://github.com/BartMelman/fact_checking
git pull origin master

# === test code === #
# Github hexaf folder: https://github.com/j6mes/fever-uclmr-system

cd fever








https://github.com/j6mes/fever-uclmr-system
